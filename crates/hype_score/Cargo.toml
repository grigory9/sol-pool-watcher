[package]
name = "hype_score"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version="1", features=["sync"] }
serde = { version="1", features=["derive"] }
solana-sdk = "3"
common_types = { path = "../common_types" }
